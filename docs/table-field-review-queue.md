# 表格字段整理任务队列

日期：2026-08-28。范围：当前正式源码中全部 10 张表；每张表独立核对，按领域分批实现和验证。

## 统一验收合同

- 必显列默认显示且不可关闭；推荐列默认显示、可关闭；备选列默认隐藏、可开启。
- 默认顺序按使用任务安排，通常为主标识、核心信息、归属或分类、状态、时间、操作；日志优先时间。操作始终最后。
- 1440px 是可读性基线，不是字段必显的判定算法。保留用户列偏好；新默认顺序在首次使用或重置后生效。
- 只有值得横向比较的字段进入列设置；原始技术 ID、长正文、完整变更和原始 User-Agent 等留在详情。
- 详情覆盖当前契约中全部允许查看的业务字段，并按业务含义分组；不包含选择框、操作列，不递归复制关联对象的全部属性。
- 保留 ManagementProTable、TanStack Query、现有权限与偏好责任层；不换表格、不新增后端、不重置已有偏好。
- 每项分别核对默认表头、可配置列、详情字段、相关行为测试；最终统一运行全量门禁和 1440/768/390px 页面验收。

## 逐表任务

| 编号 | 表格与路由 | 本项工作 | 状态 |
| --- | --- | --- | --- |
| T01 | 用户 `/organization/users` | 主标识优先，技术 ID 移出列设置；基本、组织、账号、活动字段分组并补齐详情 | 进行中 |
| T02 | 角色 `/access/roles` | 类型与成员数按语义排序，完整权限与技术 ID 留详情，核对全部契约字段 | 待处理 |
| T03 | 部门 `/organization/departments` | 名称/标识后展示规模、状态，详情核对层级和时间信息 | 待处理 |
| T04 | 岗位 `/organization/positions` | 名称/标识、归属、人数、状态、时间顺序，详情核对部门关联 | 待处理 |
| T05 | 字典类型 `/system/dictionaries` | 名称/标识、条目数、状态、时间顺序，说明留详情 | 待处理 |
| T06 | 字典项 `/system/dictionaries` | 标签/值、颜色、排序、状态、时间顺序，说明和类型关联留详情 | 待处理 |
| T07 | 公告 `/system/announcements` | 核对标题、状态、时间与批量选择；正文和技术字段留详情 | 待处理 |
| T08 | 审计日志 `/operations/audit-logs` | 时间优先，收敛技术列和大内容列；业务、请求、设备、变更详情分组 | 待处理 |
| T09 | 登录日志 `/operations/login-logs` | 时间优先，收敛追踪字段；结果、来源、设备、技术信息分组 | 待处理 |
| T10 | 生产依赖 `/system/about` | 核对包名、版本、操作顺序；只读嵌入表不强加详情和列设置 | 待处理 |

## 验证与交付

- 待记录每项的失败测试、实现、相关回归与保留的合理设计。
- 待运行全量门禁、页面验收和补丁扫描。
- 本轮页面人工验收需要单独确认浏览器操作授权；历史验收授权不复用。
- 任务创建不代表表格改造完成；源码与验收完成后才将对应项标记为完成。
